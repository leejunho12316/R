install.packages("rJava")
install.packages("memoise")
install.packages("KoNLP")
library(KoNLP)
library(dplyr)
useNIADic
txt <- readLines("hiphop.txt")
head(txt)
install.packages("stringr")
install.packages("stringr")
library(stringr)
txt<-str_replace_all(txt,"\\w"," ")
extractNoun("아나 씨발 좆같네")
library(KoNLP)
extractNoun("씨발새끼야")
ㅇ
nound <- extractNoun(txt)
nound
wordcount <- table(unlist(nound))
wordcount
df_word <- as.data.frame(wordcount,stringsAsFactors = F)
df_word
df_word <- rename(df_word,word=Var1,freq=Freq)
library(dplyr)
df_word <- rename(df_word,word=Var1,freq=Freq)
df_word
txt
txt <- readLines("hiphop.txt")
txt <- readLines("hiphop.txt")
useNIADic()
useNIADic()
1
txt <- readLines("hiphop.txt")
library(KoNLP)
library(dplyr)
useNIADic()
txt <- readLines("hiphop.txt")
library(KoNLP)
library(dplyr)
useNIADic()
txt <- readLines("hiphop.txt")
head(txt)
library(stringr)
txt <- str_replace_all(txt,"\\W"," ")
nouns <- extractNoun(txt)
wordcount <- table(unlist(nouns))
head(wordcount)
head(nouns)
df_word <- as.data.frame(wordcount,stringsAsFactors = F)
df_word <- renamd(df_word,word=Var1,freq=freq)
df_word <- rename(df_word,word=Var1,freq=Freq)
df_word
df_word <- filter(df_word(word) >= 2)
df_word <- filter(df_word, nchar(word) >= 2)
df_word
top_20 <- df_word %>% arrange(desc(freq)) %>% head(20)
top_20
install.packages("wordcloud")
library(wordcloud)
library(RColorBrewer)
pal <- brewer.pal(8."Dark2")
pal <- brewer.pal(8,"Dark2")
pal
word(words=df_word$word,
freq = df_word$freq,
min.freq = 2,
max.words = 200,
random.over=F,
rot.per=.1,
scale=c(4,0,3),
color=pal)
wordcloud(words=df_word$word,
freq = df_word$freq,
min.freq = 2,
max.words = 200,
random.over=F,
rot.per=.1,
scale=c(4,0,3),
color=pal)
warnings
wrnings()
warnings()
wordcloud(words=df_word$word,
freq = df_word$freq,
min.freq = 2,
max.words = 200,
random.over=F,
rot.per=.1,
scale=c(4,0,3),
color=pal)
wordcloud(words=df_word$word,
freq = df_word$freq,
min.freq = 2,
max.words = 200,
random.over=F,
rot.per=.1,
scale=c(4,0,3),
color=pal)
wordcloud(words=df_word$word,
freq = df_word$freq,
min.freq = 2,
max.words = 200,
random.over=F,
rot.per=.1,
scale=c(4,0,3),
color=pal)
wordcloud(words=df_word$word,
freq = df_word$freq,
min.freq = 2,
max.words = 200,
random.over=F,
rot.per=.1,
scale=c(4,0,3),
color=pal)
wordcloud(words=df_word$word,
freq = df_word$freq,
min.freq = 2,
max.words = 200,
random.over=F,
rot.per=.1,
scale=c(4,0,3),
color=pal)
set.seed(1234)
wordcloud(words=df_word$word,
freq = df_word$freq,
min.freq = 2,
max.words = 200,
random.over=F,
rot.per=.1,
scale=c(4,0,3),
color=pal)
a
a
a
a
wordcloud(words = df_word$word, freq = df_word$freq, min.freq = 2, max.words = 200, random.order = F, rot.per= .1, scale=c(4,0.3),colors=pal)
wordcloud(words = df_word$word, freq = df_word$freq, min.freq = 2, max.words = 200, random.order = F, rot.per= .1, scale=c(4,0.3),colors=pal)
wordcloud(words = df_word$word, freq = df_word$freq, min.freq = 2, max.words = 200, random.order = F, rot.per= .1, scale=c(4,0.3),colors=pal)
set.seed(2001)
wordcloud(words = df_word$word, freq = df_word$freq, min.freq = 2, max.words = 200, random.order = F, rot.per= .1, scale=c(4,0.3),colors=pal)
wordcloud(words = df_word$word, freq = df_word$freq, min.freq = 2, max.words = 200, random.order = F, rot.per= .1, scale=c(4,0.3),colors=pal)
useNIADic()
install.packages(“stringr”)
txt<-readLines("hiphop.txt")
head(txt)
txt <- str_replace_all(txt,"\\W"," ")
head(txt)
nouns <- extractNoun(txt)
wordcount <- table(unlist(nouns))
class(nouns)
class(wordcount)
df_word <- as.data.frame(wordcount,stringsAsFactors = F)
df_word <- rename(df_word,word = Var1,freq = Freq)
head(df_word)
wordcount
head(df_word)
head(df_word,20)
head(df_word,50)
df_word <- filter(df_word,nchar(word) >= 2)
head(df_word,50)
top20 <- df_word %>% arrange(desc(freq))
head(top20)
palate <- brewer.pal(8,"Dark2")
plate
palate <- brewer.pal(8,"Dark2")
palate
palate <- brewer.pal(8,"Dark2")
palate
set.seed(1052)
wordcloud(words = df_word$word,
freq = df_word$freq,
min.freq = 2,
max.words = 200,
random.order = F,
rot.per = .1,
scale = c(4, 0.3),
colors = palate
)
set.seed(1234)
wordcloud(words = df_word$word,
freq = df_word$freq,
min.freq = 2,
max.words = 200,
random.order = F,
rot.per = .1,
scale = c(4, 0.3),
colors = palate
)
palate <- brewer.pac(9,"Blues")[5:9]
palate <- brewer.pal(9,"Blues")[5:9]
palate
wordcloud(words = df_word$word,
freq = df_word$freq,
min.freq = 2,
max.words = 200,
random.order = F,
rot.per = .1,
scale = c(4, 0.3),
colors = palate
)
